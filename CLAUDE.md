# אפליקציית כדורשת (volleyball-team-app)

**קרא קודם את `ROADMAP.md`** — הוא מקור האמת לכל מה שנעשה ולמה. הקובץ הזה הוא רק
מה שקל ליפול בו.

React + Vite · Firebase (Firestore/Auth/Storage/Functions Node 22) · Vercel
Production: volleyball-team-app-green.vercel.app · Firebase: volleyball-team-app-e1a2f
סופר-אדמין: efil59@gmail.com · קבוצת הבית: `bibleumi`

## קונבנציות מסירה

- **כל מסירה מעדכנת `ROADMAP.md`.** זו לא בקשה מנומסת — זה איך שהפרויקט הזה זוכר.
- **פיצ'ר שגלוי לשחקנית מעלה `WHATS_NEW`** ב-constants.js. שינוי אדמין-בלבד, אבטחה
  או תיקון באג — **לא** מעלה.
- בדיקות ב-Incognito. שרת dev על פורט 5200.

## מלכודות שכבר עלו ביוקר

**מחיקת `setupDone` לבדה נועלת שחקנית החוצה.** `emailAuth` מנסה קודם *ליצור* חשבון,
ואם הוא כבר קיים — *להתחבר* עם מה שהוקלד. שחקנית שתבחר סיסמה חדשה במסך ההרשמה
תיחסם ב-"החשבון כבר קיים" ולא תוכל להיכנס בכלל. כל "החזרה לכניסה ראשונה" חייבת
למחוק גם את חשבון ה-Firebase — ראה `adminResetPlayerToSetup`.

**הנוכחות חיה בשלושה מקומות.** `attendance/{playerId}` (אישורים חיים),
`attendanceData` בתוך כל שורה ב-`data/archive` (מזין את הסטטיסטיקה), ו-`outcome`
+`result` על אירועים ב-`data/events` שלא אורכבו. ניקוי של אחד מהם בלבד משאיר
סטטיסטיקה ישנה שחוזרת ונראית כמו באג.

**כל transform/filter/will-change על הורה שובר את התפריט התחתון.** הוא
`position:fixed`, וכל אחד מאלה יוצר containing-block שמנתק אותו מהחלון. מעברי-מסך
משתמשים ב-opacity בלבד מסיבה זו.

**כללי Firestore מוחלים ידנית בקונסולה — אין `firestore.rules` ברפו.**
כשמוסיפים subcollection חדשה חובה כלל חדש, אחרת היא default-deny והכתיבה נכשלת
בשקט. שחקנית = אימייל פיקטיבי `{team}-p{pid}@players.bibleumi.app`.

**פריסת functions נופלת לפעמים על timeout של 10 שניות** בטעינת המודול. זה קאש קר
של הדיסק, לא באג: הרצה שנייה נטענת בשניות בודדות ועוברת. פשוט להריץ שוב.

## ⚠️ Vercel Hobby מול מכירה

האפליקציה נמכרת (`PaymentCard`, ₪39 לחודש בביט). ההנחיות של ורסל מגדירות זאת
כשימוש מסחרי, ו-Hobby מוגבל ל-non-commercial בלבד. **הפרת מדיניות משביתה את כל
הצוות** — כל שאר האפליקציות של אפי יושבות על אותו חשבון. מעבר ל-Pro לפני הלקוח
המשלם הראשון. (נרשם 4.9.26; אותה סיבה שבגללה הוסרה AdSense מ-televizia.)
