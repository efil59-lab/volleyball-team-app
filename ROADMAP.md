# 🏐 ROADMAP — אפליקציית כדורשת (bibleumi)

> ניהול משימות לקראת מסחור. נשמר ב-`efil59-lab/volleyball-team-app`.
> **עדכון אחרון:** 6 ביולי 2026

סמלים: ⏳ ממתין · 🚧 בעבודה · ✅ הושלם · 💭 רעיון

---

## 🗺️ מפת שינויים 2.0 (אושרה 6.7.26) — הכנה לעשרות קבוצות

| שלב | מה | סטטוס |
|-----|-----|:---:|
| 1 | **תשתית:** CRA→Vite · פיצול App.jsx למודולים · design tokens · ניקיונות | ✅ 6.7.26 |
| 2 | **רענון עיצובי:** פונט עברי · ניווט תחתון · RSVP בלחיצה מהבית · טיפוגרפיה | ✅ 6.7.26 |
| 3 | **נכונות בקנה מידה:** סקרים/מחיאות/התראות למסמך-פר-שחקנית ✅ · אבטחת api/chat ✅ · ErrorBoundary ✅ · ניטור errorLogs ✅ · טעינה עצלה (נבדק ונדחה) | ✅ 6.7.26 |
| 4 | **תזכורות + התראות:** Web Push (FCM) עם SW דחיפה-בלבד · תזכורת ערב-לפני + בוקר-האירוע + סיכום למנהלת · ביטול מיידי · מייל תקציר שגיאות (ממתין ל-RESEND_API_KEY) | ✅ 7.7.26 **מאומת קצה-לקצה בטלפון אמיתי** |
| 5 | **מסחור:** ניסיון-חינם 14 יום ✅ · תשלום ביט ✅ · דף נחיתה מוכר ✅ · פרטיות+תנאים ✅ · גיבוי יומי ✅ · ~~מאמן AI~~ (בוטל — החלטת אפי 7.7.26) | ✅ 7.7.26 |

### שלב 1 — פירוט (הושלם 6.7.26)
- ✅ **CRA→Vite 7:** build ב-6 שניות (היה דקות). `npm run dev/build/preview`. פורט 5200 לפיתוח.
  - `envPrefix` כולל `REACT_APP_` — משתני הסביבה הקיימים ב-Vercel עובדים ללא שינוי בדשבורד.
  - `vercel.json`: framework=vite + outputDirectory=build במפורש (דורס את preset ה-CRA בדשבורד — בלעדיו הפריסה הייתה נשברת).
  - `.env.local` מקומי (gitignored) עם הקונפיג הציבורי של Firebase — מאפשר פיתוח מקומי.
- ✅ **פיצול App.jsx (4,592 שורות) ל-14 מודולים:** `lib/` (constants·utils·db·auth·images) · `styles/` (S·tokens.css·index.css) · `components/shared` · `screens/` (gate·home·player·admin·superadmin·info) · App.jsx רזה (389 שורות). התוכן הועתק כמו-שהוא — אפס שינוי התנהגות.
- ✅ **תשתית design tokens:** `styles/tokens.css` — משתני CSS לצבעים/רווחים/טיפוגרפיה. שלב 2 יחבר אליהם את הרכיבים.
- ✅ **ESLint 9** (flat config): `npm run lint` — תופס imports חסרים ושגיאות hooks. 0 שגיאות.
- ✅ **ניקיונות:** הוסרו react-scripts, xlsx (לא בשימוש + חולשות), testing-library, web-vitals · נמחקו קבצי CRA (SW registration, reportWebVitals, logo.svg, App.css) · `npm audit`: 0 פגיעויות (היו 3).
- ✅ **נבדק:** lint נקי · build עובר · אומת בדפדפן מול נתונים אמיתיים (דף נחיתה, מסך בית בינלאומי — 13 שחקניות, טיקר, משחק היום).
- 🛟 main.jsx עדיין מבטל SW ישן (ביטוח למכשירים מתקופת CRA). יוסר כשנעבור ל-SW דחיפה-בלבד בשלב 4.

### שלב 2 — פירוט (הושלם 6.7.26) · **WHATS_NEW → 19**
- ✅ **פונט Heebo** (variable, self-hosted דרך fontsource — בלי CDN חיצוני). הוסרו כל הצהרות ה-fontFamily הישנות; הפונט יורש מ-body דרך `--font-family` ב-tokens.css.
- ✅ **ניווט תחתון** (`BottomNav` ב-components/shared): מחליף את שורת הטאבים העליונה הצפופה גם לשחקנית וגם למנהלת. 4 טאבים ראשיים + גיליון "עוד"; כשטאב מ"עוד" פעיל, המשבצת החמישית מציגה אותו. נקודה אדומה על צ'אט עם הודעות שלא נקראו (וגם על "עוד" אם פריט פנימי מסומן). `env(safe-area-inset-bottom)` לאייפון.
  - שחקנית: נוכחות · לוח · צ'אט · תמונות · עוד(תוצאות משחקים, סקר)
  - מנהלת: נוכחות · אירועים · שחקניות · הודעות · עוד(סקר, סטטיסטיקה, תמונות, הגדרות)
- ✅ **אישור הגעה בלחיצה אחת מהמסך הראשי** (מצב א' — מכשיר זכור): כפתורי מגיעה/לא מגיעה חיים על כרטיס האירוע, נשמרים מיד (`quickRSVP`), הכפתור הפעיל מלא + "נשמר ✓". שומר הערה קיימת. שחקנית לא-מחוברת (טוקן אנונימי) מופנית למסך הסיסמה — לא נחסמת בכללים.
- ✅ **טיפוגרפיה:** קלטים 14→16px (גם מונע זום אוטומטי באייפון בפוקוס שדה).
- ✅ המדריך (Help) עודכן: אישור הגעה מהמסך הראשי + הסבר התפריט התחתון.
- ✅ **נבדק** (dev, viewport מובייל 375px): ניווט תחתון בשני המסכים + גיליון "עוד" + החלפת טאבים · כרטיס RSVP מול נתוני אמת (סטטוס קיים מוצג נכון) · fallback אנונימית→מסך סיסמה בלי כתיבה · lint 0 שגיאות · build עובר.

### שלב 3 — פירוט (בעבודה — 6.7.26)
- ✅ **אבטחת api/chat** — נקודת הקצה של ה-AI הייתה פתוחה לגמרי (CORS "*", ללא אימות, מפתח Anthropic חי). אף חלק באפליקציה לא קורא לה (נבדק). הושבתה (403) עד שלב 5, שיבנה אותה מחדש עם אימות Firebase token + הגבלת קצב + הפעלה מבוקרת.
- ✅ **תיקון last-write-wins בסקרים/מחיאות/התראות** (נפרס ואומת 6.7.26):
  - הבאג: `polls`/`applause`/`personalNotifs` נשמרו כמסמך-יחיד עם read-modify-write. שתי שחקניות במקביל דרסו זו את זו — בדיוק כמו שהיה בצ'אט.
  - המבנה החדש (מסמך-פר-פריט, כמו הצ'אט/נוכחות): `polls/{id}` (הצבעה=`updateDoc` על שדה `votes.{pid}`, בטוח מקבילית) · `applause/{id}` (append) · `personalNotifs/{pid}` (`arrayUnion`). קריאה ב-`onSnapshot` בזמן-אמת; ה-UI לא השתנה.
  - התחלה נקייה (כמו הצ'אט); המסמכים הישנים `data/{polls,applause,personalNotifs}` נשמרים כגיבוי. **סקרים פתוחים ומוני-מחיאות של החודש אופסו** במעבר (מקובל — בטסטים).
  - **כללי Firestore הורחבו** (הוחלו ידנית בקונסולה): נוספו 3 בלוקים ל-`polls`/`applause`/`personalNotifs` בסגנון הקיים — מנהלת יוצרת/מוחקת סקר, שחקנית משנה **רק** את `votes` (`diff().hasOnly(['votes'])`), מחיאה חייבת `isOwnPlayer(fromId)`. הרשימה הלבנה הישנה ב-`data/{doc}` נשארה (גיבוי; מיותרת כעת).
  - אומת מול Firestore האמיתי: קריאות עוברות, כתיבה עוברת (השגיאות שנראו קודם היו ישנות ב-buffer).
- ✅ **גבול-שגיאה (ErrorBoundary) + לוכד שגיאות גלובלי** (נפרס 6.7.26): קריסת רינדור בכל מקום מציגה מסך "טעני מחדש 🏐" ידידותי במקום מסך לבן (קריטי לקהל לא-טכני). `unhandledrejection` נלכד ל-console. `lastAppError` שמור כנקודת-חיבור לניטור עתידי. אומת: האפליקציה עובדת רגיל עם ה-boundary עוטף.
- 🔵 **טעינה עצלה — נבדק ונדחה בכוונה:** ההוצאה האמיתית בקנה מידה היא רוחב-פס של תמונות (§8), וזו **כבר עצלה** — `<img>` בגלריה יורד רק כשנכנסים לטאב. מסמך אינדקס הגלריה קטן (URLs, לא base64) והקריאה זולה (~read אחד). דחיית הארכיון תשבור את טאב-הברירת-מחדל (מחיאות תלויות ב-`lastArchived`). ⇒ מיקרו-אופטימיזציה עם footgun (העלאה-לפני-טעינה דורסת גלריה) — לא שווה על שינוי טרי. אם יתגלה צורך אמיתי בקנה מידה — נחזור.
- ✅ **ניטור ביתי (בחירת אפי — במקום Sentry):** `lib/errorLog.js` כותב שגיאות ל-collection גלובלי `errorLogs` (דדופ-סשן + תקרת 20 + סינון רעש צפוי). מקורות: ErrorBoundary (קריסת מסך), `unhandledrejection`, `window.onerror`. מסך בסופר-אדמין: רשימה אדומה/ירוקה עם הודעה · מקור · מכשיר (מ-userAgent) · קבוצה · זמן · stack מתקפל · "נקה הכל". דרדוד חינני — בלי הכלל, כתיבות נבלעות והמסך מציג "אין שגיאות". כלל `errorLogs` (create=signedIn, read/delete=isSuper) הוחל ואומת (כתיבת-anon עוברת, קריאה/מחיקה חסומות). נפרס 6.7.26.

> **נותר לאפי (לא חוסם):** בדיקת-אמת של הצבעה/מחיאות עם 2 מכשירים (מוודא את תיקון ה-last-write-wins); ניקוי שורת-probe אחת ב-errorLogs דרך "נקה הכל" בסופר-אדמין.

### שלב 4 — פירוט (קוד מוכן 7.7.26, ממתין להפעלה) · **WHATS_NEW → 20 (ב-commit, טרם נדחף)**
- ✅ **4 פונקציות ענן נפרסו** (us-central1): `eventRemindersEvening` (19:00 IL — אירועי מחר, למי שטרם אישרה) · `eventRemindersMorning` (08:00 IL — אירועי היום + סיכום הגעה למנהלת) · `notifyTeamPush` (callable, מנהלת — ביטול מיידי) · `errorDigest` (שעתי — מייל תקציר שגיאות דרך Resend; בלי RESEND_API_KEY ב-functions/.env יוצא בשקט). ניקוי טוקנים מתים אוטומטי.
- ✅ **SW דחיפה-בלבד** (`public/sw.js`): push + notificationclick בלבד, **אין fetch/cache** — אפס סיכון קוד ישן. הרישום ב-main.jsx מחליף SW ישן מתקופת CRA. אומת: נרשם ומופעל ב-dev.
- ✅ **צד לקוח** (`lib/push.js` + `components/ReminderCard`): הפעלה/ביטול פר-מכשיר; טוקן נשמר ב-`teams/{t}/pushTokens/{token}` = {role, playerId, uid, ua}. מצבי קצה: אייפון-בדפדפן → "התקיני קודם"; חסימה → הסבר; אין VAPID → הכרטיס מוסתר (אומת). מיקום: טאב נוכחות (שחקנית) + טאב הגדרות (מנהלת). ביטול אירוע שולח push מיידי (fire-and-forget).
- ✅ **הופעל 7.7.26:** מפתח VAPID נוצר ומולא ב-constants.js · כלל pushTokens פורסם ואומת (כתיבה+מחיקה) · נדחף לייצור (bundle חדש + sw.js מוגש + המפתח ב-bundle — אומת). **נותר:** בדיקת קצה-לקצה בטלפון של אפי (הפעלה + ביטול-בדיקה) · אופציונלי: RESEND_API_KEY ב-functions/.env + redeploy למיילי-שגיאות.

### שלב 4 — תוספת: שליטת מנהלת בתזכורות (7.7.26)
- ✅ **פונקציה שעתית אחת** (`eventRemindersHourly`) במקום שני מתזמנים קבועים: כל שעה עגולה נבדק לכל קבוצה פעילה אם זו שעת התזכורת **שלה** (לפי `settings`). הישנות נמחקו.
- ✅ **הגדרות פר-קבוצה** (טאב הגדרות, אדמין-בלבד → בלי WHATS_NEW): מתג פעיל/כבוי (`remindersEnabled`, ברירת מחדל פעיל) · שעת ערב-לפני (`reminderEveningHour`, 16-22, ברירת מחדל 19:00) · שעת בוקר-האירוע (`reminderMorningHour`, 6-14, **ברירת מחדל 10:00** — לבקשת אפי). התראת ביטול נשארת מיידית תמיד (לא תלויה במתג).
- ✅ נפרס ואומת (UI עם ברירות מחדל נכונות; deploy הפונקציות עבר).

### שלב 4 — תוספת: דחיפת התקנה + באנר עדכון (7.7.26, לקראת השקת התזכורות ב-19:00)
- ✅ **מסך התקנה בכל כניסה עד שמתקינות** (בקשת אפי): הזיהוי לפי `display-mode: standalone` — מותקן=לא מוצג, דפדפן=מוצג בכל כניסה (עם "אמשיך בדפדפן בינתיים"). הנימוק במסך: חובה להתקנה כדי לקבל תזכורות 🔔. באנדרואיד/כרום — **כפתור התקנה אמיתי בלחיצה אחת** (`beforeinstallprompt` נלכד ב-main.jsx); באייפון — הוראות. מוצג רק בזרימת שחקנית (מנהלת/נחיתה לא מוטרדות).
- ✅ **באנר "יש גרסה חדשה"** (כמו televizia): Vite מטמיע `__BUILD_ID__` ופולט `version.json` בכל build; האפליקציה בודקת בפוקוס + כל 5 דק' — שוני=באנר 🚀 קבוע למעלה, לחיצה=reload (אין SW-cache אז רענון=קוד חדש מיד). `vercel.json`: version.json עם Cache-Control no-store. אומת ב-dev (מסך ההתקנה חוזר בכל טעינה) וב-build (version.json נפלט, id מוטמע).
- אלה שינויי מטא-UX — בלי העלאת WHATS_NEW (מסך ההתקנה הוא-עצמו ההודעה).

### תיקון באג — קבוצת-רפאים במכשירי שחקניות (7.7.26)
דווח מטלפון אמיתי: מסך בית ריק (בלי שחקניות) עם שם ברירת-המחדל. **סיבה:** המכשיר שמר `pwaTeam` ישן (קישור-בדיקה לקבוצה שנמחקה); סקריפט השחזור ב-index.html הזריק אותו לכל פתיחה, וכל הנתונים נפלו ל-fallback (שם ברירת-המחדל זהה לשם הבינלאומי — לכן נראה "תקין"). **תיקון:** לכל קבוצה אמיתית יש `meta` — כשאין meta והקבוצה אינה הבינלאומי: ניקוי pwaTeam/currentTeamId + דף נחיתה. אומת בדפדפן לשני הכיוונים. באג → בלי WHATS_NEW.

### שלב 4 — אימות קצה-לקצה + כלי אבחון (7.7.26 ערב)
- ✅ **הוכח בטלפון אמיתי (אנדרואיד/כרום):** הפעלה כשחקנית ← טוקן נרשם ← שליחת בדיקה מהשרת ← ההתראה קפצה על המסך. `sent:1`.
- 🔍 **תהליך האבחון שהוביל לזה:** ריצת 19:00 החזירה sent=0 → לוג אבחון + endpoint זמני `debugPush` חשפו tokens=[] — אף מכשיר לא השלים הפעלה (אדג' היה חסום ברמת אנדרואיד; כרום טרם הופעל). אחרי הפעלה תקינה — הכול עבד מיד. **לקח:** sent=0 ≠ באג בהכרח; קודם לבדוק כמה טוקנים רשומים.
- 🔧 **debugPush** (HTTP, us-central1): `?key=<DEBUG_PUSH_KEY>&team=X` מציג טוקנים/אירועי-מחר/טרם-ענו; `&send=1` שולח התראת-בדיקה לכל הרשומים. המפתח ב-`functions/.env` (לא ב-git — הריפו ציבורי; מפתח ישן שדלף להיסטוריה סובב ומת). להסרה כשההטמעה תתייצב.
- 📱 **שכבות חסימה שהתגלו באדג'-אנדרואיד:** גם הרשאת האפליקציה באנדרואיד וגם מתג-על בתוך אדג' — כששניהם לא פתוחים, האתר מדווח denied והרשימה ריקה. ההוראות בכרטיס הותאמו (מותקן מול דפדפן).
- ⏭️ הצ'קפוינט האוטומטי הבא: מחר 10:00 — תזכורת בוקר לטרם-ענו + סיכום למנהלת (דורש שאפי יפעיל 🔔 גם דרך הגדרות-מנהל בטלפונו).

### שלב 5א+5ב — מנוע הניסיון והתשלום (7.7.26)
**המשפך התהפך:** במקום בקשה→המתנה לאישור אפי→חזרה→pending, עכשיו **self-service מלא**:
- ✅ "🎁 נסי חינם 14 יום" → Google → `resolveAdminTeam` יוצר קבוצה **חיה מיד** (status=active, plan=trial, trialEndsAt=+14) → ישר לאשף ← הבנות נכנסות, תזכורות עובדות. הפיקוח בדיעבד: callable `notifyNewTeam` שולח push לאפי (טוקני admin של בינלאומי) + מייל (אם Resend). "כניסת מנהל" של זרה נשארת מבוי-סתום (אנטי-זבל).
- ✅ **ניסיון:** באנר צהוב בפאנל (ספירה לאחור, לחיץ) · פג-תוקף: באנר אדום + מודל תשלום נפתח אוטומטית · הבנות מקבלות LockedTeamScreen עם reason="expired" · תזכורות-ענן מדלגות על ניסיון-שפג (activeTeams).
- ✅ **PaymentCard** (components): מחיר (PAYMENT.priceText) + הוראות ביט (bitPhone) עם העתקת מספר/קוד-קבוצה; bitPhone ריק → כפתורי וואטסאפ/מייל. **⚠️ ממתין מאפי: מספר ביט + אישור מחיר (₪39 placeholder).**
- ✅ **סופר-אדמין:** תגית plan (🎁 ניסיון עד X / ⏰ פג / 💰 שולם) + כפתורי "💰 שולם" (setTeamPaid) ו"➕14" (extendTrial).
- 🔵 זרימת joinRequests הישנה מיותרת (הרשמה עצמית); הפאנל עדיין מציג בקשות-אם-יש (legacy לא מזיק). הכלי הידני "פתיחת קבוצה למנהלת" נשאר כ-fallback.
- אומת ב-dev (mock meta): באנר 7-ימים → מודל מחיר+קשר · פג → מודל אוטומטי+באנר אדום · שחקנית-בפג → מסך נעילה ייעודי. lint+build נקיים.

### שלב 5ג+5ד+גיבוי (7.7.26)
- ✅ **דף נחיתה מוכר** (LandingScreen חדש): hero עם CTA "🎁 נסי חינם 14 יום" · רשת 8 פיצ'רים · "איך מתחילים" (3 צעדים) · כרטיס מחיר (₪39/חודש, ביט, ביטול בכל עת) · שורת אמון · footer עם כניסת-מנהלת/בינלאומי + קישורי משפטי. זה החלון-ראווה לפייסבוק/ממה-נט.
- ✅ **משפטי:** `legal/legalTexts.js` (מקור-אמת לטקסטים) + `screens/legal.jsx` (רנדרר). מדיניות פרטיות — הטיוטה הותאמה למוצר רב-קבוצתי (13 סעיפים, תיקון 13, מולאו שדות: קשר efil59@gmail.com, תאריך 7.7.26). תנאי שימוש חדשים (10 סעיפים: ניסיון/תשלום/שימוש הוגן/אחריות/סיום). נגישים מ-footer הנחיתה + שורת הסכמה במסך ההרשמה. **עדיין מומלצת סקירת עו"ד לפני קנה-מידה.**
- ✅ **גיבוי יומי** (`dailyBackup`, 03:00 IL): snapshot JSON מלא של כל קבוצה (data + כל תתי-האוספים + 300 הודעות צ'אט אחרונות) + אוספים גלובליים → Storage `backups/YYYY-MM-DD/` (דלי פרטי) · שמירת 30 יום · הפעלה ידנית: debugPush&backup=1. **אומת בהרצה אמיתית** (teams=1). שחזור: הורדת JSON מהקונסולה. טיפ פריסה: במכונה הזו נדרש `FUNCTIONS_DISCOVERY_TIMEOUT=120`.
- ✅ ביט מולא: 052-4473685 (PaymentCard מציג הוראות ביט מלאות). מחיר ₪39 אושר.
- 🚫 **מאמן AI בוטל** (החלטת אפי) — api/chat נשארת מושבתת; פריט 19 בבקלוג ירד.

### המדריך למנהלת (7.7.26)
- ✅ **מקור אמת יחיד:** `content/adminGuide.js` — 10 פרקים (כל לשוניות הניהול + צעדים-ראשונים + תזכורות) ו-12 שאלות נפוצות. **לעדכן שם בלבד** כשמשתנים פיצ'רים.
- ✅ **מסך באפליקציה:** `screens/adminGuide.jsx` (שכבת-על) — כרטיס "📖 המדריך למנהלת" בראש לשונית ההגדרות, עם FAQ-אקורדיון.
- ✅ **PDF למסירה:** `public/admin-guide.pdf` (נגיש ב-/admin-guide.pdf + כפתור ⬇ בהגדרות). נבנה מאותו תוכן: סקריפט scratchpad ← HTML להדפסה ← `msedge --headless --print-to-pdf` (אין Python במכונה; Edge נותן עברית-RTL מושלמת). **לחדש את ה-PDF אחרי שינוי תוכן:** להריץ שוב את סקריפט הבנייה ולהחליף את הקובץ.
- אומת: מסך + אקורדיון + קישור PDF (200, application/pdf) + תוכן RTL מלא.

### תיקון באג — התפריט התחתון היה חבוי (6.7.26)
דווח: צריך לגלול כדי לראות את הניווט התחתון. **סיבה:** אנימציית `.screen-fade` השאירה `transform: translateY(0)` על העוטף — וכל `transform` על הורה יוצר *containing-block* שגורם ל-`position:fixed` (התפריט) להיתלות בתחתית התוכן במקום בתחתית החלון. **תיקון:** מעבר-המסך הוא כעת דעיכת-שקיפות בלבד (בלי transform) → התפריט נעוץ נכון לחלון. אומת בדפדפן (nav bottom = viewport 812, גלוי בלי גלילה). בנוסף: ריפוד תחתון של התוכן שונה ל-`calc(80px + safe-area)` (מרווח אחיד מעל התפריט בכל מכשיר). כל המודלים כבר z-index ≥240 > 230 של התפריט — נבדק שהם מעליו (bottom-sheet של פרופיל z300, כפתור "שמור" גלוי). באג → לא מעלה WHATS_NEW.

---

## 🎯 במוקד עכשיו (3 עדיפויות)

### 1. שלב 6 — גלריה: namespacing + דחיסה + הגבלה ✅
**הושלם 20 ביוני 2026.** לפני שהרבה שחקניות מעלות תמונות (עלות + דליפה).
- ✅ נתיבי Storage לפי קבוצה: `teams/{teamId}/gallery/{ts}_{name}` (היה גלובלי). תמונות ישנות נשארות בנתיב הישן ועובדות מה-URL השמור — בלי מיגרציה. מחיקה משתמשת ב-`storagePath` השמור (גם ישן וגם חדש).
- ✅ דחיסה בצד-לקוח (`compressImage`): מקס' 1280px, JPEG 0.8 — חיסכון ~10x. אם התמונה כבר קטנה — לא נוגעים.
- ✅ הגבלת 5 תמונות/שחקנית/יום (ספירה בצד-לקוח לפי `playerId`+תאריך) + מונה "נותרו X תמונות היום".
- ✅ מצב טעינה "מעלה..." על הכפתור + השבתה בזמן העלאה.
- ⚠️ **נותר ידני:** לעדכן כללי Firebase Storage (גודל מקס' 5MB + סוג image/* + נתיב חדש). הקוד נמסר בשיחה.

### 2. שלב 7 — צ'אט ל-subcollection ✅
**הושלם 20 ביוני 2026.** מוּסגר מחדש: לא אופטימיזציית ביצועים אלא **תיקון נכונוּת** —
המודל הישן (מסמך יחיד, read-modify-write) איבד הודעות בשליחה במקביל (last-write-wins).
- ✅ מעבר מ-`data/chat` (מסמך-ענק) ל-subcollection `teams/{t}/chat/{msgId}` — כל הודעה מסמך נפרד (`setDoc` עם id כשם המסמך). אין דריסה, אין אובדן הודעות.
- ✅ listener: `query(orderBy("ts","desc"), limit(200))` — מוריד רק 200 אחרונות, וכל שחקנית מקבלת רק שינויים (זול, במקום הורדת כל המסמך בכל הודעה).
- ✅ פורמט המערך נשמר זהה → ה-UI (נקודת התראה, גלילה, רינדור) לא השתנה.
- ✅ מחיקה לפי doc id (`deleteDoc`). הסרת `upd.chat` (קוד מת).
- 🛟 המסמך הישן `data/chat` נשמר כגיבוי (לא נמחק). צ'אט חדש מתחיל נקי.
- ⚠️ **נותר ידני:** להוסיף כלל Firestore ל-`teams/{t}/chat/{msgId}`. הקוד נמסר בשיחה.

### 3. לפני גביית כסף — משפטי + תשתית ⏳
- ✅ **טיוטת מדיניות פרטיות מוכנה** (`PRIVACY_POLICY_DRAFT.md`) — מותאמת לאפליקציה, 13 סעיפים, מבוססת תיקון 13. ממתינה למילוי שדות + סקירת עו"ד + חיבור כמסך (לפני מכירה).
- תנאי שימוש (טיוטה — טרם נכתב).
- בדיקת חוק הגנת הפרטיות הישראלי (תיקון 13, 14.8.2025) — התייעצות עו"ד לפני מכירה.
- שדרוג Vercel ל-Pro ($20/חודש) כשהאפליקציה מניבה הכנסה (Hobby אסור מסחרית).

---

## 📋 Backlog (לא חוסם השקה)

| # | פיצ'ר | סטטוס | הערה |
|---|--------|:---:|------|
| 6 | גרסת דמו 7 ימים | ⏳ | נדרש למכירה (נעילה אחרי תקופה) |
| 7 | Onboarding למנהל חדש | ✅ | אשף 3-צעדים + teamId קצר `vb-XXXX` + **חסם פתיחה: הזמנה לפי מייל (גישה A)**. הושלם 20.6.26 |
| 9 | גיבוי ושחזור | ⏳ | סל מיחזור 30 יום |
| 10 | מערכת תשלומים | 💭 | Stripe/PayBox — אחרי 10-20 לקוחות |
| 15 | רישום הגעה GPS | 💭 | סימון אוטומטי בהגעה לאולם |
| 19 | ~~הרחבת מאמן AI~~ | 🚫 | בוטל (7.7.26, החלטת אפי). api/chat נשארת מושבתת |
| 16 | סנכרון Google Calendar | 💭 | לוח חודשי כבר קיים; סנכרון חיצוני אם יידרש |
| 17 | אוהדים/בני משפחה | 💭 | |
| 18 | וידאו Highlights | 💭 | |

---

## ✅ הושלם (תמצית)

**אבטחה ורב-קבוצתיות (Tier 2) — מלא:**
- Email/Password אמיתי לשחקנית (אימייל פיקטיבי נסתר, uid מובטח)
- 3 Cloud Functions: איפוס סיסמה (זמנית + וואטסאפ + החלפה מאולצת), מחיקת שחקנית נקייה, מחיקת קבוצה (סופר-אדמין, אישור בהקלדת שם)
- כללי Firestore איתנים ב-3 גלים (ניהולי=מנהל, פר-שחקנית=email-matched, נעילת secrets/members/teamIndex/users)
- סופר-אדמין: רשימת קבוצות + אישור/השהיה/מחיקה · מצב pending/active · `LockedTeamScreen`
- פיצול נתונים: `attendance`/`profiles`/`secrets` פר-שחקנית
- כניסה/התנתקות נקייה (שחקנית + מנהל), הבחנת Google מול שחקנית
- קבוצה חדשה נקייה (seed ריק); שם קבוצה דינמי בכל מקום (`settings.teamName`)

**פיצ'רים לשחקנית/מנהל:**
- אישור הגעה (כן/לא/טרם ענו) + רשימות שמיות + ספירה
- וואטסאפ אוטומטי לחסרים · צ'אט בזמן אמת (נקודה מהבהבת) · סקרים · מחיאות · טיקר/הודעות
- ברכות יום הולדת (אישי + קבוצתי) · תוצאות משחקים (ניצחון/הפסד/תיקו)
- סטטיסטיקה אישית + **דירוג שחקניות קבוצתי** (% הגעה, ממוצע) · ייצוא Excel
- מסך בית אישי (Mode A/B) · לוח חודשי · ביטול אירוע + undo · ארכוב עם אימות + תזכורת
- **צ'אט — תיקון מחיקת הודעה (27.6.26):** הכותבת יכולה למחוק את הודעותיה (כפתור 🗑 הוחזר). הבעיה לא הייתה בקוד אלא בכלל Firestore — חסר היתר `delete`. כל הודעה חדשה שומרת כעת `uid` כדי שהכלל יוכל לאמת בעלות. נוסף `notify` על כשל מחיקה. שינוי באג → לא מעלה גרסה. ⚠️ נותר ידני: לעדכן כלל `teams/{t}/chat/{msgId}` — `allow delete` אם uid תואם **או** המבקש הוא מנהלת (ספק Google), כך שהמנהלת יכולה למחוק כל הודעה ולנקות ישנות ללא uid (הכלל נמסר בשיחה).
- **תיקון ארכוב כללי (27.6.26):** "ארכב הכל" ארכב גם משחק שעבר ללא תוצאה — בניגוד לכלל הארכוב הבודד. תוקן: `canArchiveEv` (עבר + לא-משחק **או** משחק עם תוצאה); הארכוב הכללי משתמש בו, ומשחקים שעברו ללא תוצאה נשארים ב-`events`. הבאנר מציג ספירת ארכוב אמיתית + שורת אזהרה "X משחקים ממתינים לתוצאה". תיקון באג → לא מעלה גרסה.
- **לוח השנה — אירועים מאורכבים נשארים (27.6.26):** בעת ארכוב, אירוע עבר מ-`events` ל-`archive` ונעלם מהלוח. כעת הלוח (גם מנהל וגם שחקנית) בנוי על `calEvents` = איחוד `events`+`archive` (ללא כפילויות לפי id), כך שאירוע שהסתיים/אורכב ממשיך להופיע. הרשימה (רשימה/לשוניות) עדיין מציגה פתוחים בלבד. תיקון תצוגה → לא מעלה גרסה.
- **סקר — צפייה במצביעות + לשוניות אירועים (27.6.26):** (1) תוקן באג שהשמות לא הופיעו — מפתחות `votes` הם מחרוזות, אז `nameOf` משווה עם `String()`. (2) צפייה במצביעות נוספה גם לצד **השחקנית** (PlayerPolls) — כפתור 👁️ "מי הצביעה" → שמות ליד כל אפשרות. פיצ'ר גלוי לשחקנית → **WHATS_NEW 18** + הערה במדריך. (3) **AdminEvents — רשימת אירועים בלשוניות:** במקום שתי מקטעים מוערמים (אימונים/משחקים) → שתי לשוניות (🏋️ אימונים / 🏆 משחקים) עם `evTab`, בסגנון "כדור" (chips ממורכזים, רקע מלא בכחול לפעיל) להבחנה ויזואלית מהתפריט הראשי רשימה/לוח. אדמין-בלבד → לא מעלה גרסה.
- **סקר — צפייה במצביעות (27.6.26):** בפאנל המנהל (AdminPolls) כפתור 👁️ "מי הצביעה" לכל סקר → מציג מתחת לכל אפשרות את שמות מי שבחרה בה (מיפוי `votes` playerId→שם). אדמין-בלבד → לא מעלה גרסה. תצוגת השחקנית נשארת אנונימית (אחוזים/ספירה בלבד).
- **עריכת אירוע למנהל (27.6.26):** בלשונית 'אירועים' → 'רשימה', לכל אימון/משחק כפתור ✏️ עריכה הפותח טופס inline (סוג · יריבה · תאריך · שעה · מיקום · הערה). שמירה דרך `upd.events`. אדמין-בלבד → לא מעלה גרסה. שינוי סוג לאימון מאפס `opponent`.
- גלריה (העלאה/מחיקה/lightbox · נתיב לפי קבוצה · דחיסה 1280px/JPEG · הגבלת 5/יום) · מסך אודות + FAQ · מסך התקנה PWA
- **מקרא לוח לחיץ (27.6.26):** כל סוג במקרא הלוח (🏋️/🏆/🎂/❌) לחיץ → מסך `LegendEventsModal` המציג את כל האירועים מאותו סוג, ממוין לפי תאריך (עתידי→ישן). משחקים מאוחדים מ-events+archive עם opponent ו-`OutcomeBadge` למשחקים שהסתיימו; ימי הולדת מ-`playerProfiles[].birthday`. רכיב משותף — קיים גם ב-`AdminEvents` וגם ב-`PlayerScreen`. אימונים/משחקים: עתידיים בלבד, הקרוב ראשון. WHATS_NEW → 17 + שאלה במדריך/FAQ.
- "זכרי אותי במכשיר" · הצגת סיסמה 👁️ · UX: מתקפלים/היררכיה/אנימציות

**תשתית:**
- React + Firebase (Firestore/Storage/Auth/Functions) + Vercel · PWA
- Service Worker מבוטל (תמיד קוד עדכני) · Budget Alert ₪25/חודש
- **Onboarding מנהלת חדשה:** אשף 3-צעדים (שם קבוצה → הוספת שחקניות → שיתוף קישור) עם מד התקדמות ומסך סיום חוגג. `teamId` קצר וקריא (`vb-XXXX`) לקישור הזמנה נקי. מופיע פעם אחת (`settings.onboardingDone`).
- **חסם פתיחת קבוצה (גישה A):** מנהלת זרה לא יכולה לפתוח קבוצה לבד. שתי דרכים: (1) **בקשת הצטרפות אוטומטית** — מנהלת נכנסת עם Google → נרשמת בקשה (`joinRequests/{email}`) → סופר-אדמין רואה רשימת בקשות ממתינות בפאנל ומאשר בלחיצה (יוצר קבוצה+הזמנה); (2) **כלי ידני** — סופר-אדמין מקליד מייל ויוצר הזמנה. ללא אישור → מסך "בקשה ממתינה". מונע קבוצות-זבל ועלות. דורש כללי Firestore ל-`invites` ול-`joinRequests`.
- **תיקון דליפת DEFAULTS:** רשימת שחקניות/אירועים/משחקים לדוגמה חלה רק על הבינלאומי — קבוצה חדשה נטענת ריקה.
- **דף נחיתה (Landing):** כניסה לכתובת חשופה (בלי `?team=`) מציגה שער ראשי — "כניסת מנהל" / "כניסה לבינלאומי" / באנר רכישה + הסבר לשחקנית. מתקן באג localStorage ישן שניווט לקבוצת-רפאים.
- **הפרדת רכישה מכניסה (anti-spam):** "כניסת מנהל" לעולם לא שולחת בקשה — חשבון לא-משויך מקבל מבוי-סתום מנומס (`NotRegisteredScreen`) עם דרך לרכישה. בקשת הצטרפות נשלחת **רק** דרך זרימת הרכישה (`PurchaseScreen`), ורק לאחר אישור מפורש. באנר שיווקי מתחלף (fade) בתחתית דף הבית של השחקנית ובדף הנחיתה. `resolveAdminTeam(user, allowRequest)`.

---

## 📝 הערות כלליות

- **ארכיטקטורה:** React (App.jsx יחיד) + Firebase + Vercel + Cloud Functions (Node 22)
- **קונבנציה:** כל פיצ'ר גלוי לשחקנית → מסך "מה חדש" (גרסה נוכחית: 18)
- **תקציב:** Firebase Blaze + Budget Alert ₪25/חודש (~$0 כרגע). Vercel Hobby (לא מסחרי)
- **מנהלים:** efil59@gmail.com (super-admin) · miri.levi1962@gmail.com
- **תוכנית מפורטת:** `MULTITENANT_PLAN.md`

<!-- token-auth check de7a760 -->
